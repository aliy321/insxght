# Whitelabel assets

Place brand images here so they are consolidated and versioned:

- favicon.svg (vector icon)
- favicon.ico (fallback icon)
- app-icon-192.png
- app-icon-512.png
- og-default.png (Open Graph default)
- logo.svg (admin/logo usage)
- logo-icon.svg (admin/icon usage)

Update paths in `src/config/whitelabel.ts` accordingly.
